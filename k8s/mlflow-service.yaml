apiVersion: v1
kind: Service
metadata:
  name: mlflow-service
spec:
  type: NodePort
  selector:
    app: mlflow-ui
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
      nodePort: 30050
